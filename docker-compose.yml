version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      APP_ENV: production
      HOST: 0.0.0.0
      PORT: 8000
      DATABASE_URL: sqlite:///./signspeak.db
      MODEL_PATH: /app/artifacts/signspeak_model.joblib
      MODEL_METADATA_PATH: /app/artifacts/model_metadata.json
      CONFIDENCE_THRESHOLD: "0.75"
      STABILITY_WINDOW: "5"
      STABILITY_MIN_COUNT: "4"
      SIGN_COOLDOWN_MS: "800"
      LOG_LEVEL: INFO
    volumes:
      - ./artifacts:/app/artifacts
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: PostgreSQL for production-like testing
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: signspeak
  #     POSTGRES_USER: signspeak
  #     POSTGRES_PASSWORD: signspeak
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #
  # volumes:
  #   postgres_data:
